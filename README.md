# Advent of Code 2025

Contains simple boilerplate along with my solutions for advent of code 2025.

## How to Run

Part of how this is structured requires data to be inside the `data` directory
with the filename `day-<day-number>.txt` (for example, [[./data/day-1.txt]])

```bash
cargo run <day-number>
```
